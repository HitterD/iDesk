export enum BackupType {
    DATABASE = 'DATABASE',
    FILES = 'FILES',
    FULL = 'FULL',
}

export enum BackupStatus {
    RUNNING = 'RUNNING',
    SUCCESS = 'SUCCESS',
    FAILED = 'FAILED',
    CANCELLED = 'CANCELLED',
}
